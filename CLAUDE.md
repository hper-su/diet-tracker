@AGENTS.md

## 食事記録の登録

「〇〇様の食事です登録しておいて」のように食事内容(kcal/PFC)を渡されて登録を頼まれたら、
`.claude/skills/add-meal-log/SKILL.md` の手順(GitHub Actions の `add-meal-log.yml` を
ドライラン→本番の順で起動)で登録する。携帯・クラウド環境からでもこの方法で登録できる。
